{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist"]
    },
    "serve": {
      "dependsOn": ["build", "^serve"],
      "cache": false
    }
  }
}
